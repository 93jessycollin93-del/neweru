{
  "name": "media-converter",
  "version": "1.0.0",
  "description": "Standalone yt-dlp + ffmpeg media converter service (runs on a VPS, called over HTTP).",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  }
}
